{
	"scripts": {
    "postinstall": "npm install --prefix backend && cd frontend && npm install --only=dev && npm install && npm run build",
    "start": "cd backend && node --loader ts-node/esm index.ts"
	}
}